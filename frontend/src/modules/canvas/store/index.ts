/**
 * Canvas Store — barrel export.
 * Re-exports from centralized store for backward compatibility.
 * Implementation lives in src/store/canvasStore.ts (centralized).
 */
export { useCanvasStore } from '@/store/canvasStore'
