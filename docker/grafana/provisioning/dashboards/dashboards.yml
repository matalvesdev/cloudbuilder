apiVersion: 1

providers:
  - name: "CloudBuilder Dashboards"
    orgId: 1
    folder: "CloudBuilder"
    folderUid: "cloudbuilder"
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      foldersFromFilesStructure: false
